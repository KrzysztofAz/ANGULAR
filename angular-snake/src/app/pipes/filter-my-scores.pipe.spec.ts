import { FilterMyScoresPipe } from './filter-my-scores.pipe';

describe('FilterMyScoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMyScoresPipe();
    expect(pipe).toBeTruthy();
  });
});
