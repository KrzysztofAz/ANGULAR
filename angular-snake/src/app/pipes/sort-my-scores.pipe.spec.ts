import { SortMyScoresPipe } from './sort-my-scores.pipe';

describe('SortMyScoresPipe', () => {
  it('create an instance', () => {
    const pipe = new SortMyScoresPipe();
    expect(pipe).toBeTruthy();
  });
});
